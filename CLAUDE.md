# 정재 포트폴리오

## 프로젝트 개요
- **프로젝트명**: 홍길동 포트폴리오
- **목적**: 디자인 작업물을 소개하는 개인 포트폴리오 사이트

## 핵심 기능
- 자기소개
- 작업물 목록
- 작업물 상세
- 로그인
- 작업물 등록 / 수정 / 삭제

## 현재 구현 상태
- `index.html` — 홈 페이지
- `about.html` — 자기소개 페이지
- `preview.html` — Supabase `test_todos` 테이블 데이터를 목록으로 보여주는 예시 페이지 (작업물 목록 기능의 참고용 프로토타입)
- `style.css` — 전체 페이지 공통 스타일 (하늘색 테마, 상단 네비게이션, 카드형 레이아웃)
- 데이터는 Supabase REST API(`fetch` + `apikey`/`Authorization` 헤더)를 통해 불러오는 방식 사용 중

핵심 기능 중 작업물 상세, 로그인, 작업물 등록/수정/삭제는 아직 구현되지 않았다.

## 작업 시 참고사항
- 새 페이지를 추가할 때는 `style.css`를 그대로 링크하고, 상단 `nav.top-nav`에 다른 페이지와 동일한 링크 구조를 맞춰서 자연스럽게 이어지도록 한다.
- 작업물 목록/상세 기능은 `preview.html`에서 사용 중인 Supabase 연동 방식(REST API + fetch)을 기반으로 확장한다.
- 로그인 기능 추가 시 Supabase Auth 사용 여부를 먼저 확인하고 진행한다.

## 폰트 규칙
- 폰트는 컬러를 navy컬러로 설정한다.